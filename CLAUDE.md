# dqn-lab (bootstrap)

This repository was created by uploading one archive. On the first session:

1. `unzip -o dqn-lab.zip && rm dqn-lab.zip` at the repo root. This replaces this file with the real `CLAUDE.md`.
2. Read the new `CLAUDE.md`, then `docs/HISTORY.md`.
3. Run the checks listed under "First session in a fresh environment" and report the results.
4. Commit everything with the message "Import dqn-lab from claude.ai conversation" and open a pull request.

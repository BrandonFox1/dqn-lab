[project]
name = "pacdqn"
version = "0.1.0"
description = "Double DQN agent for a Pac-Man-style maze game"
requires-python = ">=3.10"
dependencies = ["torch>=2.1", "numpy>=1.24"]

[project.optional-dependencies]
dev = ["pytest>=7", "Pillow>=9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
